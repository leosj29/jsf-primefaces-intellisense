export default class UserSettings {
    languages?: string[];
    primeVersion?: string;
    primeExtVersion?: string;
    omniVersion?: string;
    richVersion?: string;
    facesVersion?: string;
}
